# =============================================================================
# Mozhii RAG Data Platform - Python Dependencies
# =============================================================================
# Install using: pip install -r requirements.txt
# =============================================================================

# -----------------------------------------------------------------------------
# Core Web Framework
# -----------------------------------------------------------------------------
Flask==3.0.0                    # Lightweight WSGI web application framework
Flask-CORS==4.0.0               # Cross-Origin Resource Sharing support

# -----------------------------------------------------------------------------
# HuggingFace Integration
# -----------------------------------------------------------------------------
# ≥0.21 required for CommitOperationAdd + create_commit batch uploads.
# Batch uploading all chunks in one commit prevents HF rate-limiting when
# pushing large sets (e.g. 223 chunks at once).
huggingface-hub>=0.21.0         # Official HuggingFace Hub client library

# -----------------------------------------------------------------------------
# Environment & Configuration
# -----------------------------------------------------------------------------
python-dotenv==1.0.0            # Load environment variables from .env file

# -----------------------------------------------------------------------------
# Production Server
# -----------------------------------------------------------------------------
# gthread worker class (built-in) enables multi-threading so multiple users
# can submit chunks / trigger pushes simultaneously without blocking each other.
gunicorn==21.2.0                # Production WSGI server for deployment

# -----------------------------------------------------------------------------
# Utilities
# -----------------------------------------------------------------------------
# NOTE: 'uuid' is NOT listed here — it is part of Python's standard library.
python-dateutil==2.8.2          # Date/time utilities
